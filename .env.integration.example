# Creatify API Integration Test Configuration
# Copy this file to .env.integration and fill in your actual API credentials

# Your Creatify API credentials
# Get these from: https://video.creatify.ai/ → Settings → API
CREATIFY_API_ID=your-api-id-here
CREATIFY_API_KEY=your-api-key-here

# Optional: Override API base URL (defaults to https://api.creatify.ai)
# CREATIFY_BASE_URL=https://api.creatify.ai

# Optional: Override request timeout (defaults to 30000ms)
# CREATIFY_TIMEOUT=30000

# Test configuration
# Maximum time to wait for video generation (in milliseconds)
TEST_TIMEOUT=600000

# Directory for downloaded test files (relative to project root)
TEST_OUTPUT_DIR=tests/integration/output

# Whether to keep test files after tests complete (for debugging)
KEEP_TEST_FILES=false

# Rate limiting between API calls (in milliseconds)
API_RATE_LIMIT_DELAY=1000
